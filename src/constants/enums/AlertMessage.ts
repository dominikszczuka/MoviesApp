export enum AlertMessage {
  fetch_data = "The data has been downloaded",
  add_task = "The task has been added",
  delete_tasks = "The tasks have been deleted",
  delete_task = "The tasks has been deleted",
}
